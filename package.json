{
  "name": "@jbro/tibetan-to-phonetics",
  "version": "0.8.0",
  "description": "A naive attempt at automatically generating reliable Tibetan transliterations based on customizable sets of rules.",
  "main": "dist/tibetan-to-phonetics.umd.js",
  "module": "dist/tibetan-to-phonetics.esm.js",
  "dependencies": {
    "tibetan-normalizer": "^0.5.0",
    "tibetan-syllable-parser": "^1.0.0",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "acorn": "^7.4.1",
    "rollup": "^1.32.1",
    "jest": "^26.6.0",
    "jest-cli": "^26.6.0",
    "jest-css-modules": "^2.1.0",
    "jest-environment-jsdom-fourteen": "^1.0.1",
    "jest-serializer-html": "^7.0.0"
  },
  "scripts": {
    "clean": "rm -rf ./dist && mkdir dist",
    "build": "npm run clean && npm run build:esm && npm run build:umd",
    "build:esm": "rollup src/tibetan-to-phonetics.js --format esm --file dist/tibetan-to-phonetics.esm.js",
    "build:umd": "rollup src/tibetan-to-phonetics.js --format umd --name TibetanToPhonetics --file dist/tibetan-to-phonetics.umd.js",
    "test": "jest --colors"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
